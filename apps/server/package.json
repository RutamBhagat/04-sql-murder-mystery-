{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "hono": "^4.8.2",
    "@hono/node-server": "^1.14.4",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@04-sql-murder-mystery-/env": "workspace:*",
    "@04-sql-murder-mystery-/db": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "tsdown": "^0.16.5",
    "@04-sql-murder-mystery-/config": "workspace:*"
  }
}
